services:
  mssql:
    image: mcr.microsoft.com/azure-sql-edge:latest
    container_name: site-diary-mssql
    environment:
      ACCEPT_EULA: "1"
      MSSQL_SA_PASSWORD: "SiteDiary_Dev#2026"
    ports:
      - "1433:1433"
    volumes:
      - mssql-data:/var/opt/mssql

volumes:
  mssql-data:
